fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3
pydantic==2.10.6
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.12
httpx==0.27.2
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
playwright==1.49.1
openai==1.59.6
python-dotenv==1.0.1
feedparser==6.0.11

# RAG / Vector Search
pgvector==0.3.6

# Internet Search & Web Crawling
tavily-python==0.5.0
firecrawl-py==1.5.0
# Stealth browser + LLM-friendly extraction (uses Playwright/chromium installed above)
crawl4ai==0.7.4
# Phase 1: anti-detect Firefox (camoufox) for bot-walled portals.
# After install run: python -m camoufox fetch  (downloads the patched Firefox).
camoufox[geoip]==0.4.11

# Redis (caching + queuing)
redis[asyncio]==5.2.1

# Retry logic for external API calls
tenacity==8.5.0

# Phase 3: application document package generation
# .docx export
python-docx==1.1.2
# .pdf export (pure-python)
reportlab==4.2.5
# Rasterize PDF grant-calls to images for multimodal (step-3.7-flash) reading
pymupdf==1.24.14
